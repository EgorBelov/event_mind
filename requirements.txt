# --- Core ---
python-dotenv==1.0.1
httpx==0.27.0

# --- Backend ---
fastapi==0.110.0
uvicorn[standard]==0.29.0
jinja2

# --- DB ---
sqlalchemy==2.0.29
alembic==1.13.2
# PostgreSQL driver — нужен только если DATABASE_URL начинается с postgresql.
# Для dev на SQLite оставляется неиспользуемой зависимостью.
psycopg2-binary==2.9.9
# pgvector-extension для PostgreSQL: типы Vector + операторы <->, <=>, <#>.
# Активируется alembic-миграцией 9a1b2c3d4e5f_add_pgvector только на PG.
pgvector==0.3.2

# --- Telegram ---
aiogram==3.13.1

# --- Pydantic ---
pydantic==2.8.2
pydantic-settings==2.3.4

# --- LangChain / LangGraph / Groq ---
langchain==0.2.14
langchain-core==0.2.36
langgraph==0.2.16
langchain-groq==0.1.6

# --- Embeddings / Hybrid scoring ---
sentence-transformers==2.7.0

# --- Scheduling ---
apscheduler==3.10.4

# --- HTML parsing ---
beautifulsoup4==4.12.3
lxml==5.2.2

# --- RSS / Atom parsing ---
feedparser==6.0.11
# CA-бандл для TLS-проверки RSS-лент (иначе CERTIFICATE_VERIFY_FAILED на macOS).
# Уже тянется транзитивно через httpx — фиксируем как прямую зависимость.
certifi>=2024.2.2

# --- Tests / Lint ---
pytest==8.2.2
# Линтер (конфиг в pyproject.toml [tool.ruff]); CLAUDE.md/docs ссылаются на него.
ruff==0.6.2

# --- Docs generation ---
# scripts/regenerate_docs.py собирает .docx из текущей реализации.
python-docx==1.1.2
