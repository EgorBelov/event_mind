"""Доменное ядро аккаунтов: чистые сущности, value-objects, ошибки, события.

Account-центричная модель: `User` — аккаунт с email, к которому привязаны
каналы доставки (`UserChannel`) и настройки уведомлений
(`NotificationPreference`). Telegram — лишь один из каналов, не ось системы.
Без I/O и фреймворков.
"""
