"""Порты (абстрактные интерфейсы) прикладного слоя.

Наполняется по milestone'ам: репозитории агрегатов + `UnitOfWork` (M1),
`LLMGateway`/`EmbeddingProvider` (M2), `EventSource` (M3),
`NotificationChannel` (M5). Реализации — в `eventmind.infrastructure`.
"""
